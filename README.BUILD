Requires android-support-v4.jar to build.

In Eclipse:

1. Add android-support-v4.jar via

   Project >> Properties >> Java Build Path >> Libraries >> Add External JARs...

2. Tick android-support-v4.jar in

   Project >> Properties >> Java Build Path >> Order and Export
