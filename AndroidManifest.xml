<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.thiemonagel.myapp"
    android:versionCode="1"
    android:versionName="@string/version_string" android:installLocation="auto">

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DisplayListActivity"
            android:label="@string/title_activity_display_list" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="de.thiemonagel.myapp.MainActivity" />
        </activity>

        <activity
            android:name=".List2Activity"
            android:label="@string/title_activity_list2" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="android.app.ListActivity" />
        </activity>

        <activity
            android:name=".EntryActivity"
            android:label="@string/title_activity_entry" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="android.app.EntryActivity" />
            <!-- 
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

        </application>

</manifest>
