<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.thiemonagel.vegdroid"
    android:installLocation="auto"
    android:versionCode="2"
    android:versionName="@string/version_string" >

	<!--
	    current version distribution: https://developer.android.com/about/dashboards/index.html
	    (everything before Eclair has 0.4% market share as of 2012-12-03) 
	     7: Eclair 2.1
	     8: Froyo  2.2
	     9: Gingerbread 2.3-2.3.2 (almost unused)
	    10: Gingerbread 2.3.3-2.3.7
	    15: Ice Cream Sandwich 4.0
	    16: Jelly Bean 4.1
	    17: Jelly Bean 4.2
	-->

	<!-- "targetSdkVersion indicates the highest version of Android with which you have tested with your application." -->
    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    
    <!-- uses-permission ACCESS_FINE_LOCATION implies uses-feature android.hardware.location.gps,
         but that seems overzealous to me, so a more suitable requirement is set below -->
    <uses-feature android:name="android.hardware.location" android:required="true" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:allowBackup="false" >
        
        <activity
            android:name="de.thiemonagel.vegdroid.MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="de.thiemonagel.vegdroid.DisplayListActivity"
            android:label="@string/title_activity_display_list" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="de.thiemonagel.vegdroid.MainActivity" />
        </activity>
        <activity
            android:name="de.thiemonagel.vegdroid.List2Activity"
            android:label="@string/title_activity_list2" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="android.app.ListActivity" />
        </activity>
        <activity
            android:name="de.thiemonagel.vegdroid.EntryActivity"
            android:label="@string/title_activity_entry" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="android.app.EntryActivity" />
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name="de.thiemonagel.vegdroid.AboutActivity"
            android:label="@string/title_activity_about" >
        </activity>
    </application>

</manifest>